const buyData = {

}

export default (state = mallData, action) => {
    // if (action.type === 'ggg') {
    //     const newState = JSON.parse(JSON.stringify(state));
    //     return newState;
    // }
    return state;
}